<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Profil</title>
  </head>
  <body class="text-center">

    <h1>Page Profil</h1>

    <p>Bienvenue sur la page d'ajout d'un avatar pour le User portant l'id : <%=@user.id%></p>
    <h3>Avatar actuel</h3>
    <%if @user.avatar.attached?%>
      <%= image_tag @user.avatar, alt: 'avatar', style:'object-fit: cover; width: 200px; height: 200px;' %>
    <%else%>
      <p>=== Il n'y a pas encore d'avatar lié à cet utilisateur ===</p>
    <%end%>

    <h3>Éditer la photo</h3>
    <%= form_tag user_avatars_path(@user), multipart: true do %>
      <%= file_field_tag :avatar, class: "form-group" %>
      <%= submit_tag "Enregistrer", class:"btn btn-primary btn-sm" %>
    <% end %>

    <%= link_to "Éditer mon profil", edit_user_registration_path, class:"btn btn-primary btn-sm", role:"button" %>


  </body>
</html>
